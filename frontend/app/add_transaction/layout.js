import '../../styles/add_transaction.css';

export default function AddTransactionLayout({ children }) {
    return (
      <div>
        <section>
            { children }
        </section>
      </div>
    );
  }