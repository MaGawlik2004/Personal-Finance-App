import '../../styles/generate_raport.css';

export default function RegistrationLayout({ children }) {
    return (
      <div>
        <section>
            { children }
        </section>
      </div>
    );
  }