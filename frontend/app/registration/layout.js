import '../../styles/registration.css';

export default function RegistrationLayout({ children }) {
    return (
      <div>
        <section>
            { children }
        </section>
      </div>
    );
  }